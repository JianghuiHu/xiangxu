# Changelog

## [Unreleased]

## [1.0.1] - 2026-07-29

### Added

- 颜色替换与转透明新增应用内吸色工具，可直接点击原图取色并支持 Esc 取消。
- 颜色处理新增曝光、色温、色调、色相与 RGB/红/绿/蓝分通道曲线调整。
- 颜色处理方式改为可直接切换的 Tab，并增加曲线预览和一键还原。
- 外部模型卡新增分步骤适配方案；从模型下拉框选择外部模型时进入资料、下载、转换和验证流程，不会误启用未适配权重。
- 将 PP-MattingV2 与 u2netp 调整为两个内置离线模型，并修复本地服务器不支持 HEAD 时误判模型缺失的问题。
- 外部模型由独立 JSON 自动加入模型选择框，显示“需转换适配”状态并提供第三方下载入口。
- 新增只读“更多开源模型”目录，使用独立 JSON 维护第三方模型的许可证、适用场景、运行环境、兼容状态和外部链接。
- 外部链接统一显示第三方网站提示并在新窗口打开，不下载到像序缓存。

### Fixed

- 每次导入单张、批量或文件夹图片后，自动将本次导入的第一张设为当前预览图。
- 颜色替换、转透明、灰度和反相改为独立处理模式，不再显示或叠加基础调色与 RGB 曲线参数。
- 修复“基础调整”模式跳过像素处理，导致亮度、对比度、饱和度和透明度完全不生效的问题。
- 修复亮度、对比度、饱和度或透明度设为 0 时被错误替换成默认值的问题。
- 修复桌面端公开 pywebview 原生窗口对象导致辅助功能树无限递归、应用启动后无响应的问题。
- 缩小桌面 JavaScript API 的公开范围，避免在启动阶段扫描无关的原生对象。
- 修复本地服务器把 ONNX Runtime `.mjs` 返回为二进制 MIME，导致两个内置模型均无法实际推理的问题。
- AI 推理失败时明确显示失败状态和较长错误提示，不再以“处理已结束”掩盖错误。

### Packaging

- Windows EXE 与 macOS 离线 ZIP 统一使用 v1.0.1 版本标识。
- GitHub Release 同时提供双平台下载包与 SHA-256 校验文件。

## [1.0.0] - 2026-07-27

### Added

- 批量文件、文件夹、拖放和剪贴板图片导入。
- 图片队列、统一设置、逐图自定义设置与实时预览。
- 裁切、缩放、主体占比、九宫格定位、固定画布与形状蒙版。
- 自动去边、颜色处理、文字/图片水印和目标体积压缩。
- PNG、JPG、WebP、ZIP、Sprite Sheet 与素材清单导出。
- 用户预设保存、应用、重命名和 JSON 导入导出。
- 可选本地 AI 抠图、模型缓存、下载取消、删除与失败降级。
- 本地优先处理和安全 ZIP 导出。

### Security

- GitHub/Vercel 源码发布不包含本地 AI 模型权重、测试导出、旧备份或桌面构建产物。
- 浏览器端不使用在线 CDN、远程字体或远程图标。
