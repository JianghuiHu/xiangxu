# 像序 v1.0.0

> 万象归序，创作从容。

这是像序首个公开发布准备版本，面向需要批量整理设计素材、内容图片和前端资源的用户。

## 主要能力

- 批量导入与图片队列。
- 统一处理参数与逐图自定义参数。
- 裁切、缩放、画布、蒙版、去边、颜色和水印处理。
- PNG、JPG、WebP、ZIP 与 Sprite Sheet 导出。
- 用户预设、文件检查和安全批量导出。
- 可选的浏览器本地 AI 抠图；没有模型时基础功能仍可完整运行。

## 隐私

图片的读取、预览、处理和导出均在当前浏览器中完成。应用不会把图片上传到像序服务器。用户主动下载可选模型时，只会请求模型文件，不会携带图片。

## 模型说明

GitHub 源码和 Vercel 网页版不包含 PP-MattingV2 或 u2netp 权重。PP-MattingV2 的本地发行方式仍需确认再分发；u2netp 可由用户在界面中主动下载并保存在浏览器 IndexedDB。

## 已知限制

- GIF 仅处理第一帧。
- 大图片和大批次受浏览器可用内存限制。
- Safari 不支持直接文件夹写入时会回退到 ZIP 导出。
- AI 推理使用 CPU/WASM，首次加载和推理速度受设备性能影响。
- 手机端不是本版本的主要交互目标。
- Vercel 生产部署已就绪，用户已确认正式地址可打开；自动化执行环境连接 `*.vercel.app` 超时，未在该环境重复执行线上控制台和完整交互测试。

## 发布前待确认

- 项目开源许可证。
- 是否创建并附加桌面包到 v1.0.0 Release。

源码仓库：<https://github.com/JianghuiHu/xiangxu>

网页版：<https://xiangxu.vercel.app>
